# Prototype safety: leave false until the production content and policies are approved.
NEXT_PUBLIC_SITE_INDEXABLE=false
# When indexing is enabled this must be the non-local HTTPS canonical origin.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Use Shopify for the storefront. `mock` remains an explicitly labelled local fallback.
COMMERCE_PROVIDER=shopify
SHOPIFY_STORE_DOMAIN=
# Private token from Shopify's Headless channel. Server-side only.
SHOPIFY_STOREFRONT_ACCESS_TOKEN=
SHOPIFY_STOREFRONT_API_VERSION=2026-07

# Keep false until Shipping/Returns/Tax/Legal and the hosted checkout are approved.
SHOPIFY_CHECKOUT_ENABLED=false
# Optional custom checkout host. Leave empty while Shopify uses the myshopify.com host.
SHOPIFY_CHECKOUT_DOMAIN=

# Optional launch integrations
NEXT_PUBLIC_GA4_ID=
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
